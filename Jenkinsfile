pipeline {
//    agent {
//        docker { image 'node:7-alpine' }
//        }
    stages {
        stage('Test') {
            steps {
                sh 'export'
                sh '/usr/local/bin/docker version'
            }
        }
    }
}
